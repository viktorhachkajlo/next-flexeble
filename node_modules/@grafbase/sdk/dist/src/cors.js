"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Cors = void 0;
var Cors = /** @class */ (function () {
    function Cors(params) {
        this.params = params;
    }
    Cors.prototype.toString = function () {
        var allowedOrigins = "\n    allowedOrigins: ".concat(renderAllowedOrigins(this.params.allowedOrigins));
        var maxAge = this.params.maxAge
            ? ",\n    maxAge: ".concat(this.params.maxAge)
            : '';
        return "extend schema\n  @cors(".concat(allowedOrigins).concat(maxAge, "\n  )\n\n");
    };
    return Cors;
}());
exports.Cors = Cors;
function renderAllowedOrigins(origins) {
    if (origins === '*') {
        return '"*"';
    }
    else {
        return origins
            ? "[".concat(origins.map(function (origin) { return "\"".concat(origin, "\""); }).join(', '), "]")
            : '[]';
    }
}
